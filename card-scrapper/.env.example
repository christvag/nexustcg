# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=trading_cards_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# Scraping Configuration
SCRAPE_DELAY=1  # Delay between requests in seconds
MAX_RETRIES=3
TIMEOUT=30